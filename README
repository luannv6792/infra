                         ┌───────────────────────────────┐
                         │       Jenkins (CI/CD)         │
                         │ build/push/deploy via kubectl │
                         └──────────────┬────────────────┘
                                        │
 ┌────────────────────────────────────────────────────────────────────┐
 │                 Kubernetes Cluster (Docker Desktop)                │
 │                                                                    │
 │ Namespace: app                                                     │
 │                                                                    │
 │ ┌───────────────┐    ┌──────────────────┐    ┌────────────────┐    │
 │ │ Frontend Pod  │ →→ │ Backend Pod      │ →→ │ PostgreSQL Pod │    │
 │ │ React + Nginx │    │ FastAPI + Uvicorn│    │ StatefulSet    │    │
 │ │ port: 3000    │    │ port: 8000       │    │ port: 5432     │    │
 │ └───────────────┘    └──────────────────┘    └────────────────┘    │
 │                                                                    │
 │       ↑ NodePort                                                   │
 │   http://localhost:31000                                           │
 └────────────────────────────────────────────────────────────────────┘

project-root/
├── backend/
│   ├── Dockerfile
│   ├── main.py
│   ├── requirements.txt
│   ├── k8s/
│   │   └── backend_deployment.yml
│   └── Jenkinsfile   # ✅ Pipeline riêng cho backend
├── frontend/
│   ├── Dockerfile
│   ├── src/...
│   ├── k8s/
│   │   └── frontend_deployment.yml
│   └── Jenkinsfile   # ✅ Pipeline riêng cho frontend
└── database/
    ├── k8s/
    │   └── postgres_deployment.yml
